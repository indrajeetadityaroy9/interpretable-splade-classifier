experiment_name: "ablation_cis"
output_dir: "results/ablation/cis"

data:
  dataset_name: "sst2"
  train_samples: 2000
  test_samples: 200

model:
  name: "distilbert-base-uncased"

evaluation:
  seeds: [42]
