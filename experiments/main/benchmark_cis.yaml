experiment_name: "sst2_distilbert_cis"
output_dir: "results/main/cis"

data:
  dataset_name: "sst2"
  train_samples: 2000
  test_samples: 200

model:
  name: "distilbert-base-uncased"

evaluation:
  seeds: [42]
  explainers: ["splade", "lime", "ig", "gradient_shap", "attention", "saliency", "deeplift", "random"]

mechanistic:
  circuit_threshold: 0.01
